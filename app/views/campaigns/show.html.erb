<% content_for :title, @campaign['title'] %>

<br/>
<div class="col-md-8">
  <h4>Campaign:</h4>
  <h4><%= @campaign['title'] %></h4>
</div>

<div class="col-md-8">
  <h4>Content:</h4>
  <%= @campaign['content'].html_safe %>
</div>
